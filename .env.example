DB_USER=ministry_app
DB_PASSWORD=change_me
DB_NAME=ministry_tracking
DB_HOST=db
DB_PORT=5432

# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
PII_ENCRYPTION_KEY=

# Generate with: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=

SMTP_HOST=mail.startbaptistchurch.net
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=ministry.tracker@startbaptistchurch.net

APP_BASE_URL=http://localhost:8000

# Optional — SMS notifications are skipped entirely if any of these are
# blank. Not required for the app to run.
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=
